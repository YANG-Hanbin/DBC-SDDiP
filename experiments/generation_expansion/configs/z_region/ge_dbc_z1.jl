EXPERIMENT_CONFIG = GeExperimentConfig(
    num_workers = 5,
    dry_run = false,
    static = GeStaticConfig(
        enhancement = false,
        enforce_binary_copies = false,
        sample_count = 500,
        num_backward_samples = 1,
        count_benders_cut = false,
        opt = 0.0,
        logger_save = true,
        feasibility_tol = 1e-9,
        mip_focus = 0,
        numeric_focus = 3,
        mip_gap = 1e-6,
        disjunction_iteration_limit = -1,
        branch_selection_strategy = :MFV,
        copy_branch_policy = :adaptive,
        copy_branch_min_deviation = 1e-6,
        copy_branch_dominance_ratio = 1.0,
        copy_branch_mean_ratio = 0.9,
        copy_branch_boost = 1.0,
        enable_copy_branching = false,
        enable_surrogate_copy_split = false,
        copy_split_strategy = :surrogate_delta,
        copy_split_delta_tol = 1e-6,
        idbc_warm_pass = false,
        algorithm = :modifiedCPT,
        normalization = :SNC,
        inherit_disjunctive_cuts = true,
        improvement = false,
        terminate_time = 600.0,
        terminate_threshold = 1e-3,
        max_iter = Inf,
        time_limit = 20.0,
        epsilon = 0.125,
        results_root = "results/ge_z_region_runs",
        legacy_logger_paths = false,
        experiment_tag = "ge_zregion_dbc_z1",
    ),
    sweep = GeSweepConfig(
        cuts = Symbol[:DBC],
        realizations = Int[6, 9],
        periods = Int[6, 8],
    ),
    cut = GeCutConfig(delta = 1e-2, ell1 = 0.0, ell2 = 0.0, epsilon = 0.1),
    level_set = GeLevelSetConfig(mu = 0.9, lambda = 0.5, threshold = 1e-4, next_bound = 1e10, max_iter = 200, verbose = false),
)
